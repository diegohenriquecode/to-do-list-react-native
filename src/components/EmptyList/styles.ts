import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    alignItems: "center",
    justifyContent: "center",
    marginTop: 48,
  },

  text: {
    textAlign: "center",
    color: "#808080",
  },

  textBold: {
    marginTop: 12,
    textAlign: "center",
    color: "#808080",
    fontWeight: "bold",
  },
});
